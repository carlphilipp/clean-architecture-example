apply plugin: 'java-library'

ext.moduleName = 'slalom.example.main'

dependencies {
	implementation(
		project(':core'),
		project(':in-memory-db-simple'),
		project(':jug'),
		project(':encoder'),
	)
}
